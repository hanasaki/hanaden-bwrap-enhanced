<!-- (c) 2026-* Frederick Bloom -- CliRevision.ai-hitl.collab.md -- Hanaden AI -->

# bwrap-enhanced 0.4.1 -- CLI Revision

> **Version:** `0.0.2`
> **Status:** `COLLABORATING`
> **Created:** 2026-09-09T13:42:49.186424Z
> **Branch:** `0.4.1-a`
> **Type:** AI-HITL Collaboration Document
> **Supersedes:** [v0.0.1](20260909-013323-963842-7df1-b8be-96373f8cac1e-CliRevision.ai-hitl.collab-0.0.1.md)

---

## CLI Complexity Tracker

Measured from source + collab doc. Most recent first.

| # | SemVer | DOB (Zulu) | Subcmds | User flags | Bool flags | Graded flags | Special rules | Trend |
|---|--------|------------|---------|------------|------------|--------------|---------------|-------|
| 5 | 0.0.2 | 2026-09-09T13:42:49.186424Z | 5 | 24 | 9 (bare only) | 2 (ro\|rw) | 9 | ✅ Simplified — removed `[true\|false]`, bare-only booleans |
| 4 | 0.0.1 | 2026-09-09T09:00:00.000000Z | 5 | 24 | 9 (true\|false) | 2 (ro\|rw) | 13 | ⚠️ Contradictory bool rules |
| 3 | 0.0.1 | 2026-09-09T04:00:00.000000Z | 5 | 24 | 9 (true\|false) | 2 (ro\|rw) | 12 | ✅ Scoped to antigravity-ide only |
| 2 | 0.0.1 | 2026-09-08T21:28:00.000000Z | 5 | 24 | 9 (true\|false) | 2 (ro\|rw) | 10 | 🟡 `false`=[ERROR], 5-way gate added |
| 1 | — | pre-collab | 5 | 23 | 9 (true\|false) | 2 (ro\|rw) | 6 | — v0.4.0 baseline |

---

## Protocol Reference

<!-- TEMPLATE-EXTRACTABLE: This section is generic. -->

### Editing Rules (asymmetric)

**HITL (human) may:** Edit ANY part of this document.
**AI must:** Use ONLY comment-protocol markers to communicate. Re-read FULL document on every session.

### Item Approval

Each item ends with: `### Status: [ ] APPROVED` or `[x] APPROVED`.
Document transitions `COLLABORATING` → `AGREED` when ALL items show `[x]`.

---

## Batch: bwrap-enhanced 0.4.1 -- CLI Revision

### Table of Contents

- [Item 0: Version Constant Fix](#item-0-version-constant-fix)
- [Item 1: Revised CLI Help Output](#item-1-revised-cli-help-output)
- [Item 3: Banner Emission Spec](#item-3-banner-emission-spec)
- [Item 4: `--seed X` Addition](#item-4---seed-x-addition)
- [Item 5: DRY — Single Source of CLI Contract](#item-5-dry--single-source-of-cli-contract)
- [Item 6: Spec↔Test Sync (Option C Hybrid)](#item-6-spectest-sync-option-c-hybrid)
- [Item 7: 0.4.0 Spec Archival](#item-7-040-spec-archival)

---

## Item 0: Version Constant Fix

`_SCRIPT_VERSION` reads from `VERSION` file at runtime via `git rev-parse --show-toplevel`. DRY — no hardcoded version in source.

```bash
readonly _SCRIPT_VERSION="$(cat "$(git rev-parse --show-toplevel)/VERSION" 2>/dev/null || echo "unknown")"
```

| Source | Value |
|--------|-------|
| `VERSION` file | `0.4.1-beta` |
| Branch name | `0.4.1-a` (dynamic) |

### Status: `[x] APPROVED`

---

## Item 1: Revised CLI Help Output

### `usage_top()` — agreed text

```
Sandbox Launcher -- bwrap-enhanced.sh v0.4.1-beta (0.4.1-a @ 9d36975) 2026-09-09T04:00:00.000000Z
(c) 2026 Frederick Bloom. All rights reserved.

bwrap-enhanced.sh  SUBCOMMAND  [OPTIONS]  [ARGS]
bwrap-enhanced.sh  --help | -h
bwrap-enhanced.sh  --version | -v

  provision
    --host-real-root        PATH         (default: ~/virtual-roots; MUST NOT exist)
    --virtual-user-name     NAME         (default: sandbox_user)
    --host-real-home-parent PATH         (default: HOST_REAL_ROOT/home)
    --seed                  X            (optional, repeatable; X: antigravity-ide)
    --log-level             NAME|NUMBER  (default: INFO)
    -n, --dry-run

  fsck  [ROOT_PATH]
    --host-real-root        PATH         (default: ~/virtual-roots)
    --virtual-user-name     NAME         (default: sandbox_user)
    --log-level             NAME|NUMBER  (default: INFO)
    -n  (check only)  -a  (auto-repair)  -r  (interactive)  -f  (force)  -v  (verbose)

  start  -- CMD [ARG...]
    --host-real-root        PATH         (default: ~/virtual-roots; MUST exist)
    --virtual-user-name     NAME         (default: sandbox_user)
    --host-real-home-parent PATH         (default: HOST_REAL_ROOT/home)
    --log-level             NAME|NUMBER  (default: INFO)
    -n, --dry-run                        (resolve and print bwrap argv; no exec)
    --validate                           (validate flags and paths; no exec)
    --net-passthrough                    (enables network access)
    --env-passthrough                    (enables host environment)
    --x11-passthrough                    (enables X11; implied by wayland/gnome/kde)
    --wayland-passthrough                (enables Wayland; implies x11)
    --gnome-passthrough                  (enables GNOME desktop; implies x11)
    --kde-passthrough                    (enables KDE desktop; implies x11)
    --audio-passthrough                  (enables audio)
    --a11y-passthrough                   (enables accessibility)
    --dbus-passthrough                   (enables D-Bus; NEVER implied)
    --mise-passthrough      [ro|rw]      (enables mise; bare = ro)
    --local-bin-passthrough [ro|rw]      (enables ~/.local/bin; bare = ro)

  stop  [stub -- not yet implemented]
    --host-real-root PATH  --log-level NAME|NUMBER  -f, --force  -t, --timeout SECONDS

  ls  [stub -- not yet implemented]
    --log-level NAME|NUMBER  -a, --all  -q, --quiet  --format table|json|csv

Passthrough flags: present = enable, omit = deny (default-deny).
Graded flags: omit < ro < rw (privilege escalation order).
Duplicate flag on same command line: WARNING (not error).

Seeds:  antigravity-ide
Log levels: FATAL(100) < ERROR(200) < WARN(300) < INFO(400) < DEBUG(500) < TRACE(600)
Default: INFO(400). All output goes to stderr.
Run: bwrap-enhanced.sh SUBCOMMAND --help  for detailed subcommand help.
```

### provision detail

```
  --seed X
      Seed tooling workspace configuration into the virtual root.
      Optional, repeatable. X ∈ {antigravity-ide} (0.4.1).
      Future (0.4.2+): antigravity-cli, others.
      FATAL on unknown X value.
      Fresh provision: creates root skeleton AND seeds specified configs.
      Existing root: seeds only (no skeleton re-creation).
```

### FIVE-WAY GATE (provision + seed interaction)

```
  ROOT missing + no --seed    -> CREATE skeleton only, exit 0
  ROOT missing + --seed X     -> CREATE skeleton + SEED X, exit 0
  ROOT exists  + --seed X NEW -> SEED X only, exit 0
  ROOT exists  + --seed X DUP + identical -> WARNING (no-op), exit 0
  ROOT exists  + --seed X DUP + different -> FATAL (would corrupt), exit 2
  ROOT exists  + no --seed    -> FATAL (already provisioned), exit 2

  Multiple --seed flags: atomic-or-nothing. FATAL if any would corrupt.
```

### fsck seed behavior

fsck auto-discovers which seeds are present. No `--seed` flag on fsck.
For each discovered seed: checks integrity, reports ok/broken/missing, `-a` can auto-repair.

### Status: `[x] APPROVED`

---

## Item 3: Banner Emission Spec

Every invocation emits a one-line version banner to **stderr** before any other output.
`--version` additionally prints the same string to **stdout**.

### Agreed template

```
Sandbox Launcher -- bwrap-enhanced.sh v0.4.1-beta (0.4.1-a @ 9d36975) 2026-09-09T04:00:00.000000Z
(c) 2026 Frederick Bloom. All rights reserved.
```

If script is renamed: `Sandbox Launcher -- my-sandbox.sh v0.4.1-beta (0.4.1-a @ 9d36975) [bwrap-enhanced.sh] 2026-09-09T04:00:00.000000Z`

### Implementation

```bash
_emit_banner() {
    local _script_actual _rename_tag _git_branch _git_sha _now
    readonly _SCRIPT_CANONICAL="bwrap-enhanced.sh"
    _script_actual="$(basename "$0")"
    _rename_tag=""
    [[ "$_script_actual" != "$_SCRIPT_CANONICAL" ]] && _rename_tag=" [$_SCRIPT_CANONICAL]"
    _git_branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo 'detached')"
    _git_sha="$(git rev-parse --short HEAD 2>/dev/null || echo 'unknown')"
    _now="$(date -u '+%Y-%m-%dT%H:%M:%S.%6NZ')"

    printf 'Sandbox Launcher -- %s v%s (%s @ %s)%s %s\n' \
        "$_script_actual" "$_SCRIPT_VERSION" "$_git_branch" "$_git_sha" \
        "$_rename_tag" "$_now" >&2
    printf '(c) 2026 Frederick Bloom. All rights reserved.\n' >&2
}
```

### Tests

| Test ID | What it checks |
|---------|----------------|
| `DBAN-001` | `--help` emits banner to stderr containing `Sandbox Launcher --` and script name + version |
| `DBAN-002` | Unknown subcommand error path also has banner |
| `DBAN-003` | `--version` outputs version to stdout |

### Status: `[x] APPROVED`

---

## Item 4: `--seed X` Addition

### What changed from 0.4.0

| Aspect | 0.4.0 | 0.4.1 |
|--------|-------|-------|
| Flag name | `--ide-seed` | `--seed` |
| Argument | `PATH` (default: `PROJECT_HOME/boot`) | `X` (named enum) |
| Required? | No (had default) | Optional |
| Values | Any path | `antigravity-ide` (enum; array-capable) |
| Repeatable? | No | Yes |
| Atomicity | N/A | All-or-nothing |

### Seed idempotency (per seed X)

| Case | Behavior |
|------|----------|
| Seed X exists + would change files | FATAL (refuse to corrupt) |
| Seed X exists + identical | WARNING + no-op, exit 0 |
| Seed X absent | Silent no-op |

### Seed definitions

| Seed name | What it creates | Status |
|-----------|----------------|--------|
| `antigravity-ide` | `.agents/`, `AGENTS.md`, `GEMINI.md`, `.gemini/` | 0.4.1 |
| `antigravity-cli` | agy CLI config (TBD) | Deferred to 0.4.2 |

### Files to modify

1. `bwrap-enhanced.sh` — `usage_top()`, `usage_provision()`, provision parser, `_provision_seed()`, fsck auto-discovery
2. `ScopeVeryNarrow.bstorm-0.4.1.md` — add `--seed X` to CLI contract

### Status: `[x] APPROVED`

---

## Item 5: DRY — Single Source of CLI Contract

**SSoT:** `CONSTITUTION.md` is the supreme single source of truth.

**Derivation chain (authorship, NOT code dependency):**
```
CONSTITUTION → specs → bwrap-enhanced.sh (contains usage_top()) → help output
CONSTITUTION → specs → test scripts (independently written)
Tests call bwrap-enhanced.sh --help as a black box. Tests NEVER import usage_top().
```

**Treatment of copies:**

| File | Role | Action |
|------|------|--------|
| `CONSTITUTION.md` | Supreme SSoT | Untouched |
| `ScopeVeryNarrow.bstorm-0.4.1.md` | Design spec | Actively maintained |
| `bwrap-enhanced.sh usage_top()` | Runtime expression | Actively maintained |
| `README.md` | Public-facing | Own copy, derived from constitution |
| `site/` HTML | Marketing | Own copies (site may be used elsewhere) |
| `0.4.0 brainstorming` | Frozen/stale | Add SUPERSEDED header |

### Status: `[X] APPROVED`

---

## Item 6: Spec↔Test Sync (Option C Hybrid)

**Phase 1 only in 0.4.1** (read-only audit). Phases 2-4 deferred to 0.4.2.

Phase 1 starts AFTER Items 0-5,7 are complete (agreed: Option A timing).

### Status: `[x] APPROVED`

---

## Item 7: 0.4.0 Spec Archival

### Renamed to UUIDv7-extended

| Old name | New name |
|----------|----------|
| `0.4.0-hanaden-bwrap-enhanced-tooling-...` | `20260903-083752-000000-7554-bc10-ea3770a1c3c5-BwrapEnhancedTooling.bstorm-0.4.0.md` |

SUPERSEDED header added pointing to `ScopeVeryNarrow.bstorm-0.4.1.md`.
Test harness companion doc: NOT FOUND in this workspace — deferred.

### Status: `[x] APPROVED`

---

## Execution Checklist (after all items approved)

Execution order: TDD micro-cycles, item-by-item, serialized (no parallel agents).

- `[ ]` Item 0 — Fix `_SCRIPT_VERSION` to dynamic read from VERSION
- `[ ]` Item 3 — Restore `_emit_banner()` and call from `main()`
- `[ ]` Item 1 — Update `usage_top()` with revised help text (bare boolean flags)
- `[ ]` Item 1 — Update `usage_provision()` with `--seed X`
- `[ ]` Item 1 — Update `usage_fsck()` (no --seed, auto-discovery)
- `[ ]` Item 4 — Implement `--seed X` parsing in provision
- `[ ]` Item 4 — Implement seed auto-discovery in fsck
- `[ ]` Item 4 — Update `ScopeVeryNarrow.bstorm-0.4.1.md`
- `[ ]` Item 7 — Add SUPERSEDED header to 0.4.0 brainstorming doc
- `[ ]` Item 5 — Update secondary CLI copies to reference SSoT
- `[ ]` Item 1 — Remove `[true|false]` from help text, update to bare flags
- `[ ]` Run full test suite
- `[ ]` Item 6 — Begin Phase 1 read-only spec↔test audit

### Deferred to 0.4.2 (see BACKLOG.md)

- Item 6 Phases 2-4 (spec↔test remap, new specs, new tests)
- `--seed antigravity-cli`
